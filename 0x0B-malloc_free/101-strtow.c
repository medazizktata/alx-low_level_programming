#include "main.h"
/**
* strtow - block
* Description: splits a string into words
* @str: parameter1
* Return: pointer to pointer to char
*/
char **strtow(char *str)
{
	char *array;
	
	int i, count;

	if (str == NULL || str = "")
		return (NULL);
	count = 0;
	i = 0;
	while (str[i] != '\0')
	{
		lettercount = 0;
		while (str[i] != " ")
		{
			lettercount++;
			i++;
		}
		for (k = 0; k < j; k++)
		count++;
		words[count] = word;
		i++;
	}


}
